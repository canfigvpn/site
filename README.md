# سایت نمونه HTML/CSS

این پروژه یک صفحه‌ی ساده‌ی فارسی (RTL) با HTML و CSS است.

## اجرا

برای مشاهده در مرورگر:

```bash
python3 -m http.server 8000
```

سپس آدرس زیر را باز کنید:

- http://localhost:8000
